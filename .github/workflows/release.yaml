name: Release Build

# 触发条件：当你推送类似 "v0.1.0" 这样的 tag 时触发
on:
  push:
    tags:
      - 'v*'

env:
  APP_NAME: CurlFlow

jobs:
  # === 1. 构建 Windows 版本 ===
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23' # 根据你的 go.mod 版本调整
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build Windows App
        # -clean 确保干净构建，-upx 使用压缩(可选), -nsis 生成安装包(如果有配置)
        run: wails build -platform windows/amd64 -clean -ldflags "-s -w"

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: curlflow-windows
          path: build/bin/${{ env.APP_NAME }}.exe

  # === 2. 构建 Linux 版本 (Ubuntu) ===
  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Linux Dependencies (Wails Prerequisites)
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build Linux App
        run: wails build -platform linux/amd64 -clean -ldflags "-s -w"

      - name: Upload Linux Artifact
        uses: actions/upload-artifact@v4
        with:
          name: curlflow-linux
          path: build/bin/${{ env.APP_NAME }}

  # === 3. 构建 macOS 版本 ===
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build macOS App
        # macOS 需要生成 .app 包，使用 universal 生成通用二进制
        run: wails build -platform darwin/universal -clean -ldflags "-s -w"

      - name: Create DMG
        run: |
          echo "Listing build/bin contents for debugging:"
          ls -R build/bin
          
          # 动态查找 .app 文件
          APP_PATH=$(find build/bin -name "*.app" | head -n 1)
          if [ -z "$APP_PATH" ]; then
            echo "::error::No .app bundle found in build/bin!"
            exit 1
          fi
          APP_FILENAME=$(basename "$APP_PATH")
          echo "Found app at: $APP_PATH"
          
          # 准备一个临时目录来构建 DMG 内容
          DMG_TMP="dmg_temp"
          mkdir -p "$DMG_TMP"
          cp -R "$APP_PATH" "$DMG_TMP/"
          
          # 创建指向 /Applications 的软链接，实现拖拽安装
          ln -s /Applications "$DMG_TMP/Applications"
          
          # 使用 hdiutil 生成 DMG
          hdiutil create -volname "${{ env.APP_NAME }}" -srcfolder "$DMG_TMP" -ov -format UDZO "build/bin/${{ env.APP_NAME }}-mac-universal.dmg"
          
          # 清理临时目录
          rm -rf "$DMG_TMP"

      - name: Upload macOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: curlflow-macos
          path: build/bin/${{ env.APP_NAME }}-mac-universal.dmg

  # === 4. 发布 Release ===
  release:
    needs: [build-windows, build-linux, build-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4

      - name: Display structure of downloaded files
        run: ls -R

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            curlflow-windows/${{ env.APP_NAME }}.exe
            curlflow-linux/${{ env.APP_NAME }}
            curlflow-macos/${{ env.APP_NAME }}-mac-universal.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}